[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
startCommand = "npm start"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 100

[environment]
PORT = "3000"
NIXPACKS_pkgs = "nodejs-22"

[nixpacks]
plan = { providers = ["node"] }

