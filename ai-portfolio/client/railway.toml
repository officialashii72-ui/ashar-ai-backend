[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build"
startCommand = "npx serve -s dist -l $PORT"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 100

[environment]
PORT = "5173"
NODE_ENV = "production"
